import { StyleSheet } from 'react-native';
import { spacing } from '../../theme';

export const pictureDetailStyles = StyleSheet.create({
  root: { flex: 1, marginTop: spacing.sm },
  image: { height: '100%' },
});
